.col-md-12.badge{id: 'type_'+type.id.to_s}
  = type.name.upcase
  %span{class: "pull-right"}
    = link_to "edit", edit_type_path(type), remote: true, class: 'btn btn-xs btn-primary'
    = link_to "delete", type, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-xs btn-danger'