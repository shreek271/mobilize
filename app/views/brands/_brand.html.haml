.col-md-12.badge{id: 'brand_'+brand.id.to_s}
  = brand.name.upcase
  %span{class: "pull-right"}
    = link_to "edit", edit_brand_path(brand), remote: true, class: 'btn btn-xs btn-primary'
    = link_to "delete", brand, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-xs btn-danger'